version: "3.9"
services:
  fastapi:
    build: ./fastapi
    ports:
      - "8000:8000"
    env_file: .env

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    env_file: .env
    depends_on:
      - fastapi
